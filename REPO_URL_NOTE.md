# 发布地址配置说明

本版本已按以下 GitHub Pages 项目地址配置：

```text
https://shanghhgroup.github.io/HFNL-CPUServers/
```

因此 `_config.yml` 中使用：

```yml
url: "https://shanghhgroup.github.io"
baseurl: "/HFNL-CPUServers"
```

上传时请将本文件夹内的所有文件直接放到仓库根目录，不要额外套一层文件夹。
